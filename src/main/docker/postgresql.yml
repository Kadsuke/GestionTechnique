version: '2'
services:
  gestiontechnique-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/GestionTechnique/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=GestionTechnique
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
